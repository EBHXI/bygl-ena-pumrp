_schema-version: 3.3.0
ID: bygl-ena-pumrp
description: A simple CAP project.
version: 1.0.0
modules:
  - name: bygl-ena-pumrp-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: bygl-ena-pumrp-auth
      - name: bygl-ena-pumrp-connectivity
      - name: bygl-ena-pumrp-destination
      - name: bygl-ena-pumrp-db
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: bygl-ena-pumrp
    type: approuter.nodejs
    path: app/router
    requires:
      - name: srv-api
        group: destinations
        properties:
          forwardAuthToken: true
          name: srv-api
          url: ~{srv-url}
      - name: bygl-ena-pumrp-auth
      - name: bygl-ena-pumrp-destination
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}
    parameters:
      disk-quota: 256M
      keep-existing-routes: true
      memory: 256M
  - name: bygl-ena-pumrp-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: bygl-ena-pumrp-db
    parameters:
      buildpack: nodejs_buildpack
  - name: bygl-ena-pumrp-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: bygl-ena-pumrp-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - byglenapumrpapp.zip
          name: byglenapumrpapp
          target-path: resources/
  - name: byglenapumrpapp
    type: html5
    path: app/pumrpapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: bygl-ena-pumrp-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        oauth2-configuration:
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        tenant-mode: dedicated
        xsappname: bygl-ena-pumrp-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
    requires:
      - name: app-api
  - name: bygl-ena-pumrp-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite
  - name: bygl-ena-pumrp-destination
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
      service: destination
      service-plan: lite
  - name: bygl-ena-pumrp-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: bygl-ena-pumrp-html5-service
      service-plan: app-host
  - name: bygl-ena-pumrp-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
